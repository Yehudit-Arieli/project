# פרויקט משחק - Game Project

זהו פרויקט המבוסס על Node.js, Prisma ו-PostgreSQL המורץ בתוך Docker.

## איך מפעילים את הפרויקט?

1. **הגדרת משתני סביבה:**
   יש לשכפל את הקובץ `.env.example` ולקרוא לו `.env`, ואז לעדכן את הפרטים במידת הצורך.

2. **הרצת ה-Docker:**
   יש להריץ את הפקודה הבאה בטרמינל:
   ```bash
   docker-compose up --build